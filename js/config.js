/**
 * config.js — Configuração global do Clivon Edu
 *
 * ⚠️ Este arquivo DEVE ser carregado ANTES de auth.js no HTML.
 *
 * Em produção: substitua a URL abaixo pela URL real da sua API.
 * Em desenvolvimento local: use "http://localhost:8000"
 */
const CONFIG = {
  API_BASE: "https://clivon-api.onrender.com",
};